# Cloudflare Workers 静态站点部署
name = "seo"
compatibility_date = "2026-02-22"
main = "worker.js"

[assets]
directory = "./site"
binding = "ASSETS"

# 登録者数自增（每次浏览+1，基础32000）：
# 1. 运行: wrangler kv namespace create "VISITOR_COUNTER"
# 2. 将输出的 id 填入下方，取消注释
# [[kv_namespaces]]
# binding = "VISITOR_KV"
# id = "YOUR_KV_NAMESPACE_ID"
